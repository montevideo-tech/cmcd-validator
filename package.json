{
  "name": "root",
  "private": true,
  "scripts": {
    "build" : "lerna run build",
    "start": "lerna run start --parallel",
    "test": "lerna run test",
    "clean": "lerna clean"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.5.1"
  }
}
