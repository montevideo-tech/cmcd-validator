{
  "name": "root",
  "private": true,
  "scripts": {
    "build" : "lerna run build",
    "start": "lerna run start --parallel",
    "clean": "lerna clean"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.5.1"
  }
}
