{
  "name": "root",
  "scripts": {
    "pre-realese": "lerna version prerelease --preid pre --yes --no-private",
    "realese": "lerna version --no-private minor --yes ",

    "publish": "lerna publish from-git --no-private --yes",
    "build": "lerna run build",
    "start": "lerna run start --parallel",
    "demo": "lerna run dev --scope=cmcd-validator-demo-app",
    "test": "lerna run test",
    "clean": "lerna clean"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@lerna/publish": "^6.5.1",
    "lerna": "^6.5.1"
  }
}
