{
  "name": "root",
  "private": true,
  "scripts": {
    "build" : "lerna run build",
    "start": "lerna run start --parallel",
    "clean": "lerna clean",
    "demo": "lerna run dev --scope=cmcd-react-test-app"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.5.1"
  }
}
